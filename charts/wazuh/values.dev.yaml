replicaCount: 1
autoscaling:
  enabled: false
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
ingress:
  hosts:
    - host: dev.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []
